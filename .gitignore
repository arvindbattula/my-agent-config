.DS_Store
rules-personal/
bin/__pycache__/
rules/parallel-web-search.md
rules/pi-coding-agent.md

# Pi extension — runtime cache, auto-generated from live Azure ARM discovery
pi/extensions/azure-foundry-models.json

# Pi extension — machine-specific shim for Valar proxy (local only)
pi/extensions/valar-strip-reasoning.ts

# Rule — machine-specific URL scheme allowlist (local only)
rules/url-scheme-allowlist.md

# Pi Hindsight extension — per-machine, per-session memory state (regenerates)
.pi/

# Plan-mode extension — auto-generated auditable plan artifacts
PLAN-*.md

# Lifecycle hooks — runtime state and audit logs (per-project, regenerates)
.hook-state/
reports/
