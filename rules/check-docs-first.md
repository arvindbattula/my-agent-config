When using external libraries, frameworks, or APIs, look up their current documentation before writing code. Do not rely on training knowledge for version-specific APIs, configuration options, or CLI flags. Fetch or read the actual docs — official sites, GitHub READMEs, or package registry pages — to verify function signatures, parameter names, and default behaviors before using them.